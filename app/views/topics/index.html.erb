<div class="container">
  <div class="jumbotron">
    <div class="container">
        <div class="row">
    <h1>All Topics</h1>
  </div>
</div >
    <% @topics.each do |topic| %>
    <p><%= link_to topic.title, topic_path(topic.id) %></p>
    <% end %>
  </div>
</div>
